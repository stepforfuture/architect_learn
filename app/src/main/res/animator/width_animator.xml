<?xml version="1.0" encoding="utf-8"?>
<animator xmlns:android="http://schemas.android.com/apk/res/android"

    android:valueFrom="0"
    android:valueTo="500"
    android:valueType="intType"

    android:duration="3000"
    android:startOffset="1000"
    android:fillBefore = "false"
    android:fillAfter = "true"
    android:fillEnabled = "true"
    android:repeatMode="restart"
    android:repeatCount="0"
    android:interpolator="@android:anim/decelerate_interpolator"
    />

<!--
    android:valueFrom="0" 起始值
    android:valueTo="100" 结束值
    android:valueType="intType" 值的类型

    android:duration="3000" 动画时间
    android:startOffset="1000" 延迟播放时间
    android:fillBefore = "true" 动画播放完后，视图是否停留在开始的状态,默认为true
    android:fillAfter = "false" 动画播放完后，视图是否停留在结束的状态,优于fillBefore值,默认为false
    android:fillEnabled = "true" 是否应用fillBefore值 对fillEnabled值无影响，默认为true
    android:repeatMode="restart"
    android:repeatCount="0"
    android:interpolator="@android:anim/decelerate_interpolator" 插值器  动画变化 模式的趋势，这里设置为减速动画
-->

